from datetime import datetime, timedelta, date

# new = date(2021, 11, 22)
# ex = datetime.now().date()
# print(ex.isoweekday())
# first = new - timedelta(days=new.weekday())
# print(first + timedelta(days=6))

data = {
    1: 2,
    2: 3,
}
print(sum(data.values()))